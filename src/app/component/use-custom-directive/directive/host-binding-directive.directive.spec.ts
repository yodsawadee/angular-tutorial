import { HostBindingDirectiveDirective } from './host-binding-directive.directive';

describe('HostBindingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HostBindingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
