import { HoverMoveDirective } from './hover-move.directive';

describe('HoverMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverMoveDirective();
    expect(directive).toBeTruthy();
  });
});
