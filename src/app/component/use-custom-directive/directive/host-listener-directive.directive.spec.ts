import { HostListenerDirectiveDirective } from './host-listener-directive.directive';

describe('HostListenerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HostListenerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
