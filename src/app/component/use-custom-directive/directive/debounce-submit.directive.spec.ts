import { DebounceSubmitDirective } from './debounce-submit.directive';

describe('DebounceSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
